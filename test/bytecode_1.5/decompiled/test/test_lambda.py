palette = map((lambda a: (a, a, a)), range(256))
palette = map((lambda .0: r, g, b = .0; chr(r) + chr(g) + chr(b)), palette)
palette = map((lambda r: r), palette)

palette = lambda .0: r, g, b = .0
    r

palette = lambda .0: r = .0
    r

palette = lambda r: r

palette = ((lambda .0: r = .0; r), palette)
