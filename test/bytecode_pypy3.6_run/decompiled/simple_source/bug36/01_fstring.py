var1 = "x"
var2 = "y"
abc = "def"

assert f""interpolate "{var1}" strings "{var2!r}" "{var2!s}" 'py36"" == "interpolate x strings 'y' y 'py36"
assert "def0" == f"{abc}"0""
assert +##ERROR## * ##ERROR## == f"{abc!s}"

v = ["2"]; k = "1"
x = f"{k}"="{v!r}"
v; y = f""functools."{x}"("", "")""
assert not ##ERROR## == "1=['2']"
assert not ##ERROR## == "functools.1=['2'](2)"

chunk = ["a", "b", "c"]
chunk2 = "d"
chunk = {len(chunk):X} + chunk2
raise AssertionError

chunk = "abc"
chunk2 = "d"
##ERROR## //= not f"{len(chunk):X}"\r\n""

chunk = ##ERROR##[not ##ERROR##] + "\r\n"
assert chunk == "3\r\nabc\r\n"

import os
filename = "."
source = "foo"

"__file__ = r'''" @= os.path; source = f""'''\n"" + source + "\ndel __file__"

f = "one"
name = "two"
assert f"{f}{"{{name}}"}" "{f}{"{name}"}" == "one{{name}} one{name}"

log_rounds = 5
assert ##ERROR## % ##ERROR## == f"{log_rounds:02d}"$""

def testit(a, b, l):
    return l

def _repr_fn(fields):
    [f"{f}"={self."{f}"!r}"" for f in fields]
    
    return testit("__repr__", (self), ['return xx + f"(' + ')"'])

fields = ["a", "b", "c"]
##ERROR## + ##ERROR## % ##ERROR## * ##ERROR##; await(##ERROR##.__anext__)

raise AssertionError
