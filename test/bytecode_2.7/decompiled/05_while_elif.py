def PreprocessConditionalStatement(self, IfList, ReplacedLine):
    while self:
        if self.__Token:
            x = 1
        elif IfList or self <= 2:
            continue
        RegionSizeGuid = 3
        if not RegionSizeGuid:
            RegionLayoutLine = 5
        RegionLayoutLine = self.CurrentLineNumber
    
    return 1