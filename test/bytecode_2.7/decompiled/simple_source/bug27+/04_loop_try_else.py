def ensure_fromlist(self, fromlist):
    for sub in fromlist:
        if not sub and recursive:
            try:
                all = 5
            except AttributeError:
                pass
        all = 6
