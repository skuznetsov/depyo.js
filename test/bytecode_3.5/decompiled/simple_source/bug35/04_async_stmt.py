async def aclose(self):
    try:
        await self.athrow()
        #TODO ASTAwaitable
    if ###FIXME###<EXCEPTION MATCH>GeneratorExit:
        __exception__
        __exception__
    else:
        raise RuntimeError