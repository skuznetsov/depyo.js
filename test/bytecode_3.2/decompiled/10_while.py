def readline(self, line):
    while 1:
        if self.join_lines:
            if line:
                continue
            elif self:
                continue
            
        return line

while __name__ != "__main__":
    b = 4