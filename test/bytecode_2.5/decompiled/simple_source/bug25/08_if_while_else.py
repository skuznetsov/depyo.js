def put(item, block=True, timeout=None):
    if block:
        if timeout:
            while 1:
                if True:
                    if item:
                        block = 1
            
        else:
            block = 5
    
    elif item:
        block = False
