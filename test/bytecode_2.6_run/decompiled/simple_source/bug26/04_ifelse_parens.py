def _hash_add(fields):
    for ##ERROR## in fields:
        if f is None:
            pass
        
        if f:
            pass
    flds = 4[f]
    return flds

assert _hash_add([None,
    True, False, 3]) == [None,
    True, 3]
