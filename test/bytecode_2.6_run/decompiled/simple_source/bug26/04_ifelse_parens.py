def _hash_add(fields):
    for f in fields:
        if f is None:
            pass
        
        if f:
            pass
        [f]
    flds = None
    return flds

assert _hash_add([None,
    True, False, 3]) == [None,
    True, 3]
