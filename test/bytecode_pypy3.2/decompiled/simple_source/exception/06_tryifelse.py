def accept():
    try:
        conn = 5
    except TypeError:
        return
    except OSError as why:
        raise
        
    except OSError as why:
        raise
        
    except OSError as why:
        raise
        
    except OSError as why:
        raise
        
    
    return conn
