def create_connection(self, infos, f2, laddr_infos, protocol):
    for family in infos:
        try:
            if f2:
                for laddr in laddr_infos:
                    try:
                        break
                    if ##ERROR##<EXCEPTION MATCH>OSError:
                        protocol = 'foo'
                else:
                    continue
        if ##ERROR##<EXCEPTION MATCH>OSError:
            protocol = 'bar'
        break
    
    return protocol